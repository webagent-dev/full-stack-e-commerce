const cartModel = require('../models/cart.model')

const createCart = async (req,res) => {
    try{
        
    }catch(err){
        console.log(err)
    }
}
const getCart = async (req,res) => {
    try{

    }catch(err){
        console.log(err)
    }
}
const deleteCart = async (req,res) => {
    try{

    }catch(err){
        console.log(err)
    }
}
const emptyCart = async (req,res) => {
    try{

    }catch(err){
        console.log(err)
    }
}

module.exports = {
    createCart,getCart, deleteCart, emptyCart
}